<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.ims.sql">

    <select id="ims_mt_categories_model_mapping_selectBeanByModel" parameterType="HashMap" resultType="com.voyageone.batch.ims.modelbean.CategoryModelMapBean">
        SELECT
        id,
        channel_id,
        platform_id,
        model,
        category_code
        FROM voyageone_ims.ims_mt_categories_model_mapping
        WHERE
        channel_id=#{channel_id}
        AND
        platform_id=#{platform_id}
        AND
        model=#{model}
    </select>

</mapper>
