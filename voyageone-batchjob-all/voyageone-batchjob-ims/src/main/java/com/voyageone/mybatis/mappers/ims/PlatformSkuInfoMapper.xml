<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.ims.sql">
	
	<select id="ims_selectSkuInfoByPropId" parameterType="Map" resultType="com.voyageone.batch.ims.modelbean.PlatformSkuInfoBean">
		SELECT
			cart_id,
			prop_id,
			sku_type
		FROM voyageone_ims.ims_mt_platform_sku_info
		WHERE prop_id = #{prop_id}
		AND cart_id = #{cart_id}
	</select>

	<insert id="ims_insertSkuInfo">
		INSERT INTO voyageone_ims.ims_mt_platform_sku_info
		(cart_id, prop_id, sku_type)
		VALUES (#{cart_id}, #{prop_id}, #{sku_type})
	</insert>
	
</mapper>
