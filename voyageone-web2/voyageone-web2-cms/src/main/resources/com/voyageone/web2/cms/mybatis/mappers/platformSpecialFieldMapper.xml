<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.web2.cms.sql">
    <select id="cms_mt_platform_special_field_selectSpecialMappingType" resultType="string">
        SELECT type
        FROM voyageone_cms2.cms_mt_platform_special_field
        WHERE cartId = #{cartId}
              AND catId = #{platformCategoryId}
              AND fieldId = #{propertyId}
    </select>
</mapper>