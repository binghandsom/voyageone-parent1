angular.module("${parent}",["${modules[parent].join('","')}"]);
angular.module("${parent}",["${modules[parent].join('","')}"]);
angular.module("${parent}",["${modules[parent].join('","')}"]);
angular.module("${parent}",["${modules[parent].join('","')}"]);
return angular.module("${pref}",["${parents.join('","')}"])