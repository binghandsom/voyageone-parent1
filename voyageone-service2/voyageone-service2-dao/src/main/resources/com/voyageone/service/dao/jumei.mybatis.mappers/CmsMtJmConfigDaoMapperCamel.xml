<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsMtJmConfigDao">
    <resultMap id="ResultMap" type="CmsMtJmConfigModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select `id`,`channel_id`,`key`,`value`,`created`,`creater`,`modified`,`modifier`        from cms_mt_jm_config
        <where>
                <if test="id != null">
                        AND `id` = #{id}
                    </if>
                                <if test="channelId != null">
                        AND `channel_id` = #{channelId}
                    </if>
                                <if test="key != null">
                        AND `key` = #{key}
                    </if>
                                <if test="value != null">
                        AND `value` = #{value}
                    </if>
                                <if test="created != null">
                        AND `created` = #{created}
                    </if>
                                <if test="creater != null">
                        AND `creater` = #{creater}
                    </if>
                                <if test="modified != null">
                        AND `modified` = #{modified}
                    </if>
                                <if test="modifier != null">
                        AND `modifier` = #{modifier}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select `id`,`channel_id`,`key`,`value`,`created`,`creater`,`modified`,`modifier` from cms_mt_jm_config 
        <where>
                
                    <if test="id != null">
                        AND `id` = #{id}
                    </if> 
                
                    <if test="channelId != null">
                        AND `channel_id` = #{channelId}
                    </if> 
                
                    <if test="key != null">
                        AND `key` = #{key}
                    </if> 
                
                    <if test="value != null">
                        AND `value` = #{value}
                    </if> 
                
                    <if test="created != null">
                        AND `created` = #{created}
                    </if> 
                
                    <if test="creater != null">
                        AND `creater` = #{creater}
                    </if> 
                
                    <if test="modified != null">
                        AND `modified` = #{modified}
                    </if> 
                
                    <if test="modifier != null">
                        AND `modifier` = #{modifier}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`channel_id`,`key`,`value`,`created`,`creater`,`modified`,`modifier` from cms_mt_jm_config where id=#{id}
    </select>

     <update id="update">
        update cms_mt_jm_config
        set

            `channel_id`=#{channelId}
            ,`key`=#{key}
            ,`value`=#{value}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_jm_config
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_mt_jm_config
        (
              `channel_id`,
              `key`,
              `value`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{channelId},
              #{key},
              #{value},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>