<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.cms.CmsMtCustomWordDao">
    <resultMap id="ResultMap" type="CmsMtCustomWordModel">

    </resultMap>

    <select id="selectList" resultMap="ResultMap">

        select `id`,`word_name`,`word_desc`,`created`,`creater`,`modified`,`modifier` from cms_mt_custom_word
        <where>
            <if test="id != null">
                AND `id` = #{id}
            </if>
            <if test="wordName != null">
                AND `word_name` = #{wordName}
            </if>
            <if test="wordDesc != null">
                AND `word_desc` = #{wordDesc}
            </if>
            <if test="created != null">
                AND `created` = #{created}
            </if>
            <if test="creater != null">
                AND `creater` = #{creater}
            </if>
            <if test="modified != null">
                AND `modified` = #{modified}
            </if>
            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select `id`,`word_name`,`word_desc`,`created`,`creater`,`modified`,`modifier` from cms_mt_custom_word
        <where>

            <if test="id != null">
                AND `id` = #{id}
            </if>

            <if test="wordName != null">
                AND `word_name` = #{wordName}
            </if>

            <if test="wordDesc != null">
                AND `word_desc` = #{wordDesc}
            </if>

            <if test="created != null">
                AND `created` = #{created}
            </if>

            <if test="creater != null">
                AND `creater` = #{creater}
            </if>

            <if test="modified != null">
                AND `modified` = #{modified}
            </if>

            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
        limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`word_name`,`word_desc`,`created`,`creater`,`modified`,`modifier` from cms_mt_custom_word where id=#{id}
    </select>

    <update id="update">
        update cms_mt_custom_word
        set

            `word_name`=#{wordName}
            ,`word_desc`=#{wordDesc}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_custom_word
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_mt_custom_word
        (
              `word_name`,
              `word_desc`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{wordName},
              #{wordDesc},
        now(),
              #{creater},
        now(),
        #{creater}
        )
    </insert>
</mapper>