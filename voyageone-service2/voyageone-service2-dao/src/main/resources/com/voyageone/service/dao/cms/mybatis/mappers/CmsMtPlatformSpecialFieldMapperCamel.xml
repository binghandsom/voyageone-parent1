<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.cms.CmsMtPlatformSpecialFieldDao">
    <resultMap id="ResultMap" type="CmsMtPlatformSpecialFieldModel">

    </resultMap>

    <select id="selectList" resultMap="ResultMap">

        select `seq`,`cartId`,`catId`,`fieldId`,`type`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_platform_special_field
        <where>
            <if test="id != null">
                AND `id` = #{id}
            </if>
            <if test="cartId != null">
                AND `cartId` = #{cartId}
            </if>
            <if test="catId != null">
                AND `catId` = #{catId}
            </if>
            <if test="fieldId != null">
                AND `fieldId` = #{fieldId}
            </if>
            <if test="type != null">
                AND `type` = #{type}
            </if>
            <if test="created != null">
                AND `created` = #{created}
            </if>
            <if test="creater != null">
                AND `creater` = #{creater}
            </if>
            <if test="modified != null">
                AND `modified` = #{modified}
            </if>
            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select `id`,`cartId`,`catId`,`fieldId`,`type`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_platform_special_field
        <where>

            <if test="seq != null">
                AND `seq` = #{seq}
            </if>

            <if test="cartId != null">
                AND `cartId` = #{cartId}
            </if>

            <if test="catId != null">
                AND `catId` = #{catId}
            </if>

            <if test="fieldId != null">
                AND `fieldId` = #{fieldId}
            </if>

            <if test="type != null">
                AND `type` = #{type}
            </if>

            <if test="created != null">
                AND `created` = #{created}
            </if>

            <if test="creater != null">
                AND `creater` = #{creater}
            </if>

            <if test="modified != null">
                AND `modified` = #{modified}
            </if>

            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
        limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`cartId`,`catId`,`fieldId`,`type`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_platform_special_field where id=#{id}
    </select>

    <update id="update">
        update cms_mt_platform_special_field
        set

        `id`=#{id}
            ,`cartId`=#{cartId}
            ,`catId`=#{catId}
            ,`fieldId`=#{fieldId}
            ,`type`=#{type}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_platform_special_field
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_mt_platform_special_field
        (
              `cartId`,
              `catId`,
              `fieldId`,
              `type`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{cartId},
              #{catId},
              #{fieldId},
              #{type},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>