<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmImagesDao">
    <resultMap id="ResultMap" type="CmsBtJmImagesModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,channel_id,cms_bt_jm_product_id,product_code,image_type_name,image_type,image_index,origin_url,jm_url,syn_flg,created,creater,modified,modifier        from cms_bt_jm_images
    </select>
    <select id="select" resultMap="ResultMap">
        select id,channel_id,cms_bt_jm_product_id,product_code,image_type_name,image_type,image_index,origin_url,jm_url,syn_flg,created,creater,modified,modifier from cms_bt_jm_images where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_images
        set

            `channel_id`=#{channelId}
            ,`cms_bt_jm_product_id`=#{cmsBtJmProductId}
            ,`product_code`=#{productCode}
            ,`image_type_name`=#{imageTypeName}
            ,`image_type`=#{imageType}
            ,`image_index`=#{imageIndex}
            ,`origin_url`=#{originUrl}
            ,`jm_url`=#{jmUrl}
            ,`syn_flg`=#{synFlg}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_images
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_images
        (
              `channel_id`,
              `cms_bt_jm_product_id`,
              `product_code`,
              `image_type_name`,
              `image_type`,
              `image_index`,
              `origin_url`,
              `jm_url`,
              `syn_flg`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{channelId},
              #{cmsBtJmProductId},
              #{productCode},
              #{imageTypeName},
              #{imageType},
              #{imageIndex},
              #{originUrl},
              #{jmUrl},
              #{synFlg},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>