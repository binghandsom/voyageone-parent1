<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.JmBtPromotionImportTaskDao">
    <resultMap id="ResultMap" type="com.voyageone.service.model.jumei.JmBtPromotionImportTaskModel">
        
    </resultMap>
	
    <select id="getList" resultMap="ResultMap">

        select id,promotion_id,file_name,file_path,error_msg,success_rows,failures_rows,failures_path,error_code,begin_time,end_time,creater,created        from jm_bt_promotion_import_task
    </select>
    <select id="get" resultMap="ResultMap">
        select id,promotion_id,file_name,file_path,error_msg,success_rows,failures_rows,failures_path,error_code,begin_time,end_time,creater,created from jm_bt_promotion_import_task where id=#{id}
    </select>

     <update id="update">
        update jm_bt_promotion_import_task
        set

            promotion_id=#{promotionId}
            ,file_name=#{fileName}
            ,file_path=#{filePath}
            ,error_msg=#{errorMsg}
            ,success_rows=#{successRows}
            ,failures_rows=#{failuresRows}
            ,failures_path=#{failuresPath}
            ,error_code=#{errorCode}
            ,begin_time=#{beginTime}
            ,end_time=#{endTime}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  jm_bt_promotion_import_task
        where id=#{id}

    </delete>

    <insert id="create">
        INSERT INTO jm_bt_promotion_import_task
        (
              promotion_id,
              file_name,
              file_path,
              error_msg,
              success_rows,
              failures_rows,
              failures_path,
              error_code,
              begin_time,
              end_time,
              creater,
              created
        )
        VALUES(
              #{promotionId},
              #{fileName},
              #{filePath},
              #{errorMsg},
              #{successRows},
              #{failuresRows},
              #{failuresPath},
              #{errorCode},
              #{beginTime},
              #{endTime},
              #{creater},
              #{created}
        )
    </insert>
</mapper>