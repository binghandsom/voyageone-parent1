<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.JmBtPromotionSkuDao">
    <resultMap id="ResultMap" type="com.voyageone.service.model.jumei.JmBtPromotionSkuModel">
        
    </resultMap>
	
    <select id="getList" resultMap="ResultMap">

        select id,promotion_id,product_code,jm_sku_no,sku,upc,jm_size,market_price,discount,deal_price,error_msg,synch_state,creater,created,modified,modifier        from jumei2.jm_bt_promotion_sku
    </select>
    <select id="get" resultMap="ResultMap">
        select * from jumei2.jm_bt_promotion_sku where id=#{id}
    </select>

     <update id="update">
        update jumei2.jm_bt_promotion_sku
        set

            promotion_id=#{promotionId}
            ,product_code=#{productCode}
            ,jm_sku_no=#{jmSkuNo}
            ,sku=#{sku}
            ,upc=#{upc}
            ,jm_size=#{jmSize}
            ,market_price=#{marketPrice}
            ,discount=#{discount}
            ,deal_price=#{dealPrice}
            ,error_msg=#{errorMsg}
            ,synch_state=#{synchState}
            ,modified=#{modified}
            ,modifier=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  jumei2.jm_bt_promotion_sku
        where id=#{id}

    </delete>

    <insert id="create">
        INSERT INTO jumei2.jm_bt_promotion_sku
        (
              promotion_id,
              product_code,
              jm_sku_no,
              sku,
              upc,
              jm_size,
              market_price,
              discount,
              deal_price,
              error_msg,
              synch_state,
              creater,
              created,
              modified,
              modifier
        )
        VALUES(
              #{promotionId},
              #{productCode},
              #{jmSkuNo},
              #{sku},
              #{upc},
              #{jmSize},
              #{marketPrice},
              #{discount},
              #{dealPrice},
              #{errorMsg},
              #{synchState},
              #{creater},
              #{created},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>