<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.voyageone.service.daoext.core.AdminResourceDaoExt" >
    <resultMap type="com.voyageone.service.bean.com.AdminResourceBean" id="comResourceBeanMap">
    </resultMap>

    <select id="selectResByUser" resultMap="comResourceBeanMap" >
        SELECT id, res_name , res_key, res_type, parent_id, res_url,icon , active ,
        description, created, creater, modified, modifier, application, channel_id, user_id
        from view_user_res
        WHERE active = 1
        AND user_id =  #{userId}
        GROUP BY res_key, user_id
    </select>

    <select id="selectResByRoles" resultMap="comResourceBeanMap" >
        SELECT id, res_name , res_key, res_type, parent_id, res_url, `level`, icon , active ,
        description, created, creater, modified, modifier, application, channel_id, COUNT (DISTINCT role_id) as cnt
        from view_user_res
        WHERE active = 1
        AND
        where `role_id` in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
        GROUP BY res_key
    </select>



</mapper>