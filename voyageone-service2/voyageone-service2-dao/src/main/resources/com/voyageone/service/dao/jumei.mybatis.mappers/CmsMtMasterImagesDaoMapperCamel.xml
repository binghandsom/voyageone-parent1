<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsMtMasterImagesDao">
    <resultMap id="ResultMap" type="com.voyageone.service.model.jumei.CmsMtMasterImagesModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,cart_id,channel_id,image_key_1,image_key_2,image_key_3,data_type,image_index,value1,value2,syn_flg,error_message,created,creater,modified,modifier        from cms_mt_master_images
    </select>
    <select id="select" resultMap="ResultMap">
        select id,cart_id,channel_id,image_key_1,image_key_2,image_key_3,data_type,image_index,value1,value2,syn_flg,error_message,created,creater,modified,modifier from cms_mt_master_images where id=#{id}
    </select>

     <update id="update">
        update cms_mt_master_images
        set

            cart_id=#{cartId}
            ,channel_id=#{channelId}
            ,image_key_1=#{imageKey1}
            ,image_key_2=#{imageKey2}
            ,image_key_3=#{imageKey3}
            ,data_type=#{dataType}
            ,image_index=#{imageIndex}
            ,value1=#{value1}
            ,value2=#{value2}
            ,syn_flg=#{synFlg}
            ,error_message=#{errorMessage}
            ,modified=#{modified}
            ,modifier=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_master_images
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_mt_master_images
        (
              cart_id,
              channel_id,
              image_key_1,
              image_key_2,
              image_key_3,
              data_type,
              image_index,
              value1,
              value2,
              syn_flg,
              error_message,
              created,
              creater,
              modified,
              modifier
        )
        VALUES(
              #{cartId},
              #{channelId},
              #{imageKey1},
              #{imageKey2},
              #{imageKey3},
              #{dataType},
              #{imageIndex},
              #{value1},
              #{value2},
              #{synFlg},
              #{errorMessage},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>