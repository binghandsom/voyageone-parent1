<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmSkuDao">
    <resultMap id="ResultMap" type="com.voyageone.service.model.jumei.CmsBtJmSkuModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,deal_id,product_code,cms_bt_jm_product_id,sku,jm_spu_no,jm_sku_no,upc,cms_size,jm_size,synch_state,creater,created,modified,modifier        from cms_bt_jm_sku
    </select>
    <select id="select" resultMap="ResultMap">
        select id,deal_id,product_code,cms_bt_jm_product_id,sku,jm_spu_no,jm_sku_no,upc,cms_size,jm_size,synch_state,creater,created,modified,modifier from cms_bt_jm_sku where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_sku
        set

            deal_id=#{dealId}
            ,product_code=#{productCode}
            ,cms_bt_jm_product_id=#{cmsBtJmProductId}
            ,sku=#{sku}
            ,jm_spu_no=#{jmSpuNo}
            ,jm_sku_no=#{jmSkuNo}
            ,upc=#{upc}
            ,cms_size=#{cmsSize}
            ,jm_size=#{jmSize}
            ,synch_state=#{synchState}
            ,modified=#{modified}
            ,modifier=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_sku
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_sku
        (
              deal_id,
              product_code,
              cms_bt_jm_product_id,
              sku,
              jm_spu_no,
              jm_sku_no,
              upc,
              cms_size,
              jm_size,
              synch_state,
              creater,
              created,
              modified,
              modifier
        )
        VALUES(
              #{dealId},
              #{productCode},
              #{cmsBtJmProductId},
              #{sku},
              #{jmSpuNo},
              #{jmSkuNo},
              #{upc},
              #{cmsSize},
              #{jmSize},
              #{synchState},
              #{creater},
              #{created},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>