<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsMtTemplateImagesDao">
    <resultMap id="ResultMap" type="CmsMtTemplateImagesModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,platform_id,channel_id,template_type,image_template_url,syn_flg,created,creater,modified,modifier        from cms_mt_template_images
        <where>
                <if test="id != null">
                        AND id = #{id}
                    </if>
                                <if test="platform_id != null">
                        AND id = #{platform_id}
                    </if>
                                <if test="channel_id != null">
                        AND id = #{channel_id}
                    </if>
                                <if test="template_type != null">
                        AND id = #{template_type}
                    </if>
                                <if test="image_template_url != null">
                        AND id = #{image_template_url}
                    </if>
                                <if test="syn_flg != null">
                        AND id = #{syn_flg}
                    </if>
                                <if test="created != null">
                        AND id = #{created}
                    </if>
                                <if test="creater != null">
                        AND id = #{creater}
                    </if>
                                <if test="modified != null">
                        AND id = #{modified}
                    </if>
                                <if test="modifier != null">
                        AND id = #{modifier}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select id,platform_id,channel_id,template_type,image_template_url,syn_flg,created,creater,modified,modifier from cms_mt_template_images 
        <where>
                
                    <if test="id != null">
                        AND id = #{id}
                    </if> 
                
                    <if test="platform_id != null">
                        AND id = #{platform_id}
                    </if> 
                
                    <if test="channel_id != null">
                        AND id = #{channel_id}
                    </if> 
                
                    <if test="template_type != null">
                        AND id = #{template_type}
                    </if> 
                
                    <if test="image_template_url != null">
                        AND id = #{image_template_url}
                    </if> 
                
                    <if test="syn_flg != null">
                        AND id = #{syn_flg}
                    </if> 
                
                    <if test="created != null">
                        AND id = #{created}
                    </if> 
                
                    <if test="creater != null">
                        AND id = #{creater}
                    </if> 
                
                    <if test="modified != null">
                        AND id = #{modified}
                    </if> 
                
                    <if test="modifier != null">
                        AND id = #{modifier}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select id,platform_id,channel_id,template_type,image_template_url,syn_flg,created,creater,modified,modifier from cms_mt_template_images where id=#{id}
    </select>

     <update id="update">
        update cms_mt_template_images
        set

            `platform_id`=#{platformId}
            ,`channel_id`=#{channelId}
            ,`template_type`=#{templateType}
            ,`image_template_url`=#{imageTemplateUrl}
            ,`syn_flg`=#{synFlg}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_template_images
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_mt_template_images
        (
              `platform_id`,
              `channel_id`,
              `template_type`,
              `image_template_url`,
              `syn_flg`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{platformId},
              #{channelId},
              #{templateType},
              #{imageTemplateUrl},
              #{synFlg},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>