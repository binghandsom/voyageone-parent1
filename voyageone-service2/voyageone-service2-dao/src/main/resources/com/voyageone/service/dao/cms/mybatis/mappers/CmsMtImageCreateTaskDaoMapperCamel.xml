<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.cms.CmsMtImageCreateTaskDao">
    <resultMap id="ResultMap" type="CmsMtImageCreateTaskModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select `id`,`name`,`state`,`created`,`creater`,`modified`,`modifier`,`begin_time`,`end_time`        from cms_mt_image_create_task
        <where>
                <if test="id != null">
                        AND `id` = #{id}
                    </if>
                                <if test="name != null">
                        AND `name` = #{name}
                    </if>
                                <if test="state != null">
                        AND `state` = #{state}
                    </if>
                                <if test="created != null">
                        AND `created` = #{created}
                    </if>
                                <if test="creater != null">
                        AND `creater` = #{creater}
                    </if>
                                <if test="modified != null">
                        AND `modified` = #{modified}
                    </if>
                                <if test="modifier != null">
                        AND `modifier` = #{modifier}
                    </if>
                                <if test="beginTime != null">
                        AND `begin_time` = #{beginTime}
                    </if>
                                <if test="endTime != null">
                        AND `end_time` = #{endTime}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select `id`,`name`,`state`,`created`,`creater`,`modified`,`modifier`,`begin_time`,`end_time` from cms_mt_image_create_task 
        <where>
                
                    <if test="id != null">
                        AND `id` = #{id}
                    </if> 
                
                    <if test="name != null">
                        AND `name` = #{name}
                    </if> 
                
                    <if test="state != null">
                        AND `state` = #{state}
                    </if> 
                
                    <if test="created != null">
                        AND `created` = #{created}
                    </if> 
                
                    <if test="creater != null">
                        AND `creater` = #{creater}
                    </if> 
                
                    <if test="modified != null">
                        AND `modified` = #{modified}
                    </if> 
                
                    <if test="modifier != null">
                        AND `modifier` = #{modifier}
                    </if> 
                
                    <if test="beginTime != null">
                        AND `begin_time` = #{beginTime}
                    </if> 
                
                    <if test="endTime != null">
                        AND `end_time` = #{endTime}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`name`,`state`,`created`,`creater`,`modified`,`modifier`,`begin_time`,`end_time` from cms_mt_image_create_task where id=#{id}
    </select>

     <update id="update">
        update cms_mt_image_create_task
        set

            `name`=#{name}
            ,`state`=#{state}
            ,`modifier`=#{modifier}
            ,`begin_time`=#{beginTime}
            ,`end_time`=#{endTime}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_image_create_task
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_mt_image_create_task
        (
              `name`,
              `state`,
              `created`,
              `creater`,
              `modified`,
              `modifier`,
              `begin_time`,
              `end_time`
        )
        VALUES(
              #{name},
              #{state},
              #{created},
              #{creater},
              #{modified},
              #{modifier},
              #{beginTime},
              #{endTime}
        )
    </insert>
</mapper>