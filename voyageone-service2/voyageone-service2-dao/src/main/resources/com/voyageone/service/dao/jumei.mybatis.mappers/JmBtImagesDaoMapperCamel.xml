<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.JmBtImagesDao">
    <resultMap id="ResultMap" type="com.voyageone.service.model.jumei.JmBtImagesModel">
        
    </resultMap>
	
    <select id="getList" resultMap="ResultMap">

        select id,channel_id,Product_Code,image_type_name,image_type,image_index,origin_url,jm_url,syn_flg,created,creater,modified,modifier        from jm_bt_images
    </select>
    <select id="get" resultMap="ResultMap">
        select id,channel_id,Product_Code,image_type_name,image_type,image_index,origin_url,jm_url,syn_flg,created,creater,modified,modifier from jm_bt_images where id=#{id}
    </select>

     <update id="update">
        update jm_bt_images
        set

            channel_id=#{channelId}
            ,Product_Code=#{ProductCode}
            ,image_type_name=#{imageTypeName}
            ,image_type=#{imageType}
            ,image_index=#{imageIndex}
            ,origin_url=#{originUrl}
            ,jm_url=#{jmUrl}
            ,syn_flg=#{synFlg}
            ,modified=#{modified}
            ,modifier=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  jm_bt_images
        where id=#{id}

    </delete>

    <insert id="create">
        INSERT INTO jm_bt_images
        (
              channel_id,
              Product_Code,
              image_type_name,
              image_type,
              image_index,
              origin_url,
              jm_url,
              syn_flg,
              created,
              creater,
              modified,
              modifier
        )
        VALUES(
              #{channelId},
              #{ProductCode},
              #{imageTypeName},
              #{imageType},
              #{imageIndex},
              #{originUrl},
              #{jmUrl},
              #{synFlg},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>