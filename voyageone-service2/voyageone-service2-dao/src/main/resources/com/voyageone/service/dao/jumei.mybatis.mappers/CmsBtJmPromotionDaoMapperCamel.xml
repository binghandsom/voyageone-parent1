<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmPromotionDao">
    <resultMap id="ResultMap" type="CmsBtJmPromotionModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,channel_id,name,activity_pc_id,activity_app_id,cms_bt_jm_master_brand_Id,cms_bt_jm_master_brand_Name,brand,category,activity_end,pre_period_start,pre_period_end,shipping_system_id,created,creater,modified,modifier,activity_start        from cms_bt_jm_promotion
    </select>
    <select id="select" resultMap="ResultMap">
        select id,channel_id,name,activity_pc_id,activity_app_id,cms_bt_jm_master_brand_Id,cms_bt_jm_master_brand_Name,brand,category,activity_end,pre_period_start,pre_period_end,shipping_system_id,created,creater,modified,modifier,activity_start from cms_bt_jm_promotion where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_promotion
        set

            `channel_id`=#{channelId}
            ,`name`=#{name}
            ,`activity_pc_id`=#{activityPcId}
            ,`activity_app_id`=#{activityAppId}
            ,`cms_bt_jm_master_brand_Id`=#{cmsBtJmMasterBrandId}
            ,`cms_bt_jm_master_brand_Name`=#{cmsBtJmMasterBrandName}
            ,`brand`=#{brand}
            ,`category`=#{category}
            ,`activity_end`=#{activityEnd}
            ,`pre_period_start`=#{prePeriodStart}
            ,`pre_period_end`=#{prePeriodEnd}
            ,`shipping_system_id`=#{shippingSystemId}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
            ,`activity_start`=#{activityStart}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_promotion
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_promotion
        (
              `channel_id`,
              `name`,
              `activity_pc_id`,
              `activity_app_id`,
              `cms_bt_jm_master_brand_Id`,
              `cms_bt_jm_master_brand_Name`,
              `brand`,
              `category`,
              `activity_end`,
              `pre_period_start`,
              `pre_period_end`,
              `shipping_system_id`,
              `created`,
              `creater`,
              `modified`,
              `modifier`,
              `activity_start`
        )
        VALUES(
              #{channelId},
              #{name},
              #{activityPcId},
              #{activityAppId},
              #{cmsBtJmMasterBrandId},
              #{cmsBtJmMasterBrandName},
              #{brand},
              #{category},
              #{activityEnd},
              #{prePeriodStart},
              #{prePeriodEnd},
              #{shippingSystemId},
              #{created},
              #{creater},
              #{modified},
              #{modifier},
              #{activityStart}
        )
    </insert>
</mapper>