<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsMtJmCategoryDao">
    <resultMap id="ResultMap" type="CmsMtJmCategoryModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,name,level,parent_category_id,seo_keywords,seo_description,created,creater,modified,modifier        from cms_mt_jm_category
    </select>
    <select id="select" resultMap="ResultMap">
        select id,name,level,parent_category_id,seo_keywords,seo_description,created,creater,modified,modifier from cms_mt_jm_category where id=#{id}
    </select>

     <update id="update">
        update cms_mt_jm_category
        set

            `name`=#{name}
            ,`level`=#{level}
            ,`parent_category_id`=#{parentCategoryId}
            ,`seo_keywords`=#{seoKeywords}
            ,`seo_description`=#{seoDescription}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_jm_category
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_mt_jm_category
        (
              `name`,
              `level`,
              `parent_category_id`,
              `seo_keywords`,
              `seo_description`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{name},
              #{level},
              #{parentCategoryId},
              #{seoKeywords},
              #{seoDescription},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>