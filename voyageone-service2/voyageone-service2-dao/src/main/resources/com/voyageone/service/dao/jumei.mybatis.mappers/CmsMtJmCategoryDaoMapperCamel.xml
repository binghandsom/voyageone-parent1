<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsMtJmCategoryDao">
    <resultMap id="ResultMap" type="CmsMtJmCategoryModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,name,level,parent_category_id,seo_keywords,seo_description,created,creater,modified,modifier        from cms_mt_jm_category
        <where>
                <if test="id != null">
                        AND id = #{id}
                    </if>
                                <if test="name != null">
                        AND id = #{name}
                    </if>
                                <if test="level != null">
                        AND id = #{level}
                    </if>
                                <if test="parent_category_id != null">
                        AND id = #{parent_category_id}
                    </if>
                                <if test="seo_keywords != null">
                        AND id = #{seo_keywords}
                    </if>
                                <if test="seo_description != null">
                        AND id = #{seo_description}
                    </if>
                                <if test="created != null">
                        AND id = #{created}
                    </if>
                                <if test="creater != null">
                        AND id = #{creater}
                    </if>
                                <if test="modified != null">
                        AND id = #{modified}
                    </if>
                                <if test="modifier != null">
                        AND id = #{modifier}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select id,name,level,parent_category_id,seo_keywords,seo_description,created,creater,modified,modifier from cms_mt_jm_category 
        <where>
                
                    <if test="id != null">
                        AND id = #{id}
                    </if> 
                
                    <if test="name != null">
                        AND id = #{name}
                    </if> 
                
                    <if test="level != null">
                        AND id = #{level}
                    </if> 
                
                    <if test="parent_category_id != null">
                        AND id = #{parent_category_id}
                    </if> 
                
                    <if test="seo_keywords != null">
                        AND id = #{seo_keywords}
                    </if> 
                
                    <if test="seo_description != null">
                        AND id = #{seo_description}
                    </if> 
                
                    <if test="created != null">
                        AND id = #{created}
                    </if> 
                
                    <if test="creater != null">
                        AND id = #{creater}
                    </if> 
                
                    <if test="modified != null">
                        AND id = #{modified}
                    </if> 
                
                    <if test="modifier != null">
                        AND id = #{modifier}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select id,name,level,parent_category_id,seo_keywords,seo_description,created,creater,modified,modifier from cms_mt_jm_category where id=#{id}
    </select>

     <update id="update">
        update cms_mt_jm_category
        set

            `name`=#{name}
            ,`level`=#{level}
            ,`parent_category_id`=#{parentCategoryId}
            ,`seo_keywords`=#{seoKeywords}
            ,`seo_description`=#{seoDescription}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_mt_jm_category
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_mt_jm_category
        (
              `name`,
              `level`,
              `parent_category_id`,
              `seo_keywords`,
              `seo_description`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{name},
              #{level},
              #{parentCategoryId},
              #{seoKeywords},
              #{seoDescription},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>