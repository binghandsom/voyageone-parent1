<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.cms.CmsMtChannelCategoryConfigDao">
    <resultMap id="ResultMap" type="com.voyageone.service.model.cms.CmsMtChannelCategoryConfigModel">

    </resultMap>

    <select id="selectList" resultMap="ResultMap">

        select `id`,`channel_id`,`category_id`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_channel_category_config
        <where>
            <if test="id != null">
                AND `id` = #{id}
            </if>
            <if test="channelId != null">
                AND `channel_id` = #{channelId}
            </if>
            <if test="categoryId != null">
                AND `category_id` = #{categoryId}
            </if>
            <if test="created != null">
                AND `created` = #{created}
            </if>
            <if test="creater != null">
                AND `creater` = #{creater}
            </if>
            <if test="modified != null">
                AND `modified` = #{modified}
            </if>
            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select `id`,`channel_id`,`category_id`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_channel_category_config
        <where>

            <if test="id != null">
                AND `id` = #{id}
            </if>

            <if test="channelId != null">
                AND `channel_id` = #{channelId}
            </if>

            <if test="categoryId != null">
                AND `category_id` = #{categoryId}
            </if>

            <if test="created != null">
                AND `created` = #{created}
            </if>

            <if test="creater != null">
                AND `creater` = #{creater}
            </if>

            <if test="modified != null">
                AND `modified` = #{modified}
            </if>

            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
        limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`channel_id`,`category_id`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_channel_category_config where id=#{id}
    </select>

    <update id="update">
        update cms_mt_channel_category_config
        set

            `channel_id`=#{channelId}
            ,`category_id`=#{categoryId}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from cms_mt_channel_category_config
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_mt_channel_category_config
        (
              `channel_id`,
              `category_id`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{channelId},
              #{categoryId},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>