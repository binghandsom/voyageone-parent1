<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.cms.CmsBtDataAmountDao">
    <resultMap id="ResultMap" type="CmsBtDataAmountModel">

    </resultMap>

    <select id="selectList" resultMap="ResultMap">

        select `id`,`channel_id`,`amount_name`,`amount_val`,`comment`,`created`,`creater`,`modified`,`modifier` from
        cms_bt_data_amount
        <where>
            <if test="id != null">
                AND `id` = #{id}
            </if>
            <if test="channelId != null">
                AND `channel_id` = #{channelId}
            </if>
            <if test="amountName != null">
                AND `amount_name` = #{amountName}
            </if>
            <if test="amountVal != null">
                AND `amount_val` = #{amountVal}
            </if>
            <if test="comment != null">
                AND `comment` = #{comment}
            </if>
            <if test="created != null">
                AND `created` = #{created}
            </if>
            <if test="creater != null">
                AND `creater` = #{creater}
            </if>
            <if test="modified != null">
                AND `modified` = #{modified}
            </if>
            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select `id`,`channel_id`,`amount_name`,`amount_val`,`comment`,`created`,`creater`,`modified`,`modifier` from
        cms_bt_data_amount
        <where>

            <if test="id != null">
                AND `id` = #{id}
            </if>

            <if test="channelId != null">
                AND `channel_id` = #{channelId}
            </if>

            <if test="amountName != null">
                AND `amount_name` = #{amountName}
            </if>

            <if test="amountVal != null">
                AND `amount_val` = #{amountVal}
            </if>

            <if test="comment != null">
                AND `comment` = #{comment}
            </if>

            <if test="created != null">
                AND `created` = #{created}
            </if>

            <if test="creater != null">
                AND `creater` = #{creater}
            </if>

            <if test="modified != null">
                AND `modified` = #{modified}
            </if>

            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
        limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`channel_id`,`amount_name`,`amount_val`,`comment`,`created`,`creater`,`modified`,`modifier` from cms_bt_data_amount where id=#{id}
    </select>

    <update id="update">
        update cms_bt_data_amount
        set

            `channel_id`=#{channelId}
            ,`amount_name`=#{amountName}
            ,`amount_val`=#{amountVal}
            ,`comment`=#{comment}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_data_amount
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_bt_data_amount
        (
              `channel_id`,
              `amount_name`,
              `amount_val`,
              `comment`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{channelId},
              #{amountName},
              #{amountVal},
              #{comment},
        now(),
              #{creater},
        now(),
        #{creater}
        )
    </insert>
</mapper>