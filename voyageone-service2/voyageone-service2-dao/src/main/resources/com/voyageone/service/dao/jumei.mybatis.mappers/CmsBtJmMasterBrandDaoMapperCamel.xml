<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmMasterBrandDao">
    <resultMap id="ResultMap" type="CmsBtJmMasterBrandModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,name,jm_master_brand_id,en_name,created,creater,modified,modifier        from cms_bt_jm_master_brand
        <where>
                <if test="id != null">
                        AND id = #{id}
                    </if>
                                <if test="name != null">
                        AND name = #{name}
                    </if>
                                <if test="jmMasterBrandId != null">
                        AND jm_master_brand_id = #{jmMasterBrandId}
                    </if>
                                <if test="enName != null">
                        AND en_name = #{enName}
                    </if>
                                <if test="created != null">
                        AND created = #{created}
                    </if>
                                <if test="creater != null">
                        AND creater = #{creater}
                    </if>
                                <if test="modified != null">
                        AND modified = #{modified}
                    </if>
                                <if test="modifier != null">
                        AND modifier = #{modifier}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select id,name,jm_master_brand_id,en_name,created,creater,modified,modifier from cms_bt_jm_master_brand 
        <where>
                
                    <if test="id != null">
                        AND id = #{id}
                    </if> 
                
                    <if test="name != null">
                        AND name = #{name}
                    </if> 
                
                    <if test="jmMasterBrandId != null">
                        AND jm_master_brand_id = #{jmMasterBrandId}
                    </if> 
                
                    <if test="enName != null">
                        AND en_name = #{enName}
                    </if> 
                
                    <if test="created != null">
                        AND created = #{created}
                    </if> 
                
                    <if test="creater != null">
                        AND creater = #{creater}
                    </if> 
                
                    <if test="modified != null">
                        AND modified = #{modified}
                    </if> 
                
                    <if test="modifier != null">
                        AND modifier = #{modifier}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select id,name,jm_master_brand_id,en_name,created,creater,modified,modifier from cms_bt_jm_master_brand where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_master_brand
        set

            `name`=#{name}
            ,`jm_master_brand_id`=#{jmMasterBrandId}
            ,`en_name`=#{enName}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_master_brand
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_master_brand
        (
              `name`,
              `jm_master_brand_id`,
              `en_name`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{name},
              #{jmMasterBrandId},
              #{enName},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>