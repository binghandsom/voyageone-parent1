<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmPromotionProductDao">
    <resultMap id="ResultMap" type="CmsBtJmPromotionProductModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,channel_id,cms_bt_jm_promotion_id,cms_bt_jm_product_id,product_code,jm_hash_id,limit,market_price,synch_state,creater,created,modified,modifier,state,app_id,pc_id,deal_price,discount,error_msg        from cms_bt_jm_promotion_product
    </select>
    <select id="select" resultMap="ResultMap">
        select id,channel_id,cms_bt_jm_promotion_id,cms_bt_jm_product_id,product_code,jm_hash_id,limit,market_price,synch_state,creater,created,modified,modifier,state,app_id,pc_id,deal_price,discount,error_msg from cms_bt_jm_promotion_product where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_promotion_product
        set

            `channel_id`=#{channelId}
            ,`cms_bt_jm_promotion_id`=#{cmsBtJmPromotionId}
            ,`cms_bt_jm_product_id`=#{cmsBtJmProductId}
            ,`product_code`=#{productCode}
            ,`jm_hash_id`=#{jmHashId}
            ,`limit`=#{limit}
            ,`market_price`=#{marketPrice}
            ,`synch_state`=#{synchState}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
            ,`state`=#{state}
            ,`app_id`=#{appId}
            ,`pc_id`=#{pcId}
            ,`deal_price`=#{dealPrice}
            ,`discount`=#{discount}
            ,`error_msg`=#{errorMsg}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_promotion_product
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_promotion_product
        (
              `channel_id`,
              `cms_bt_jm_promotion_id`,
              `cms_bt_jm_product_id`,
              `product_code`,
              `jm_hash_id`,
              `limit`,
              `market_price`,
              `synch_state`,
              `creater`,
              `created`,
              `modified`,
              `modifier`,
              `state`,
              `app_id`,
              `pc_id`,
              `deal_price`,
              `discount`,
              `error_msg`
        )
        VALUES(
              #{channelId},
              #{cmsBtJmPromotionId},
              #{cmsBtJmProductId},
              #{productCode},
              #{jmHashId},
              #{limit},
              #{marketPrice},
              #{synchState},
              #{creater},
              #{created},
              #{modified},
              #{modifier},
              #{state},
              #{appId},
              #{pcId},
              #{dealPrice},
              #{discount},
              #{errorMsg}
        )
    </insert>
</mapper>