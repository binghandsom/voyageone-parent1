<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmPromotionProductDao">
    <resultMap id="ResultMap" type="CmsBtJmPromotionProductModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,channel_id,cms_bt_jm_promotion_id,cms_bt_jm_product_id,product_code,jm_hash_id,limit,market_price,deal_price,synch_state,creater,created,modified,modifier,state,app_id,pc_id,discount,error_msg        from cms_bt_jm_promotion_product
        <where>
                <if test="id != null">
                        AND id = #{id}
                    </if>
                                <if test="channel_id != null">
                        AND id = #{channel_id}
                    </if>
                                <if test="cms_bt_jm_promotion_id != null">
                        AND id = #{cms_bt_jm_promotion_id}
                    </if>
                                <if test="cms_bt_jm_product_id != null">
                        AND id = #{cms_bt_jm_product_id}
                    </if>
                                <if test="product_code != null">
                        AND id = #{product_code}
                    </if>
                                <if test="jm_hash_id != null">
                        AND id = #{jm_hash_id}
                    </if>
                                <if test="limit != null">
                        AND id = #{limit}
                    </if>
                                <if test="market_price != null">
                        AND id = #{market_price}
                    </if>
                                <if test="deal_price != null">
                        AND id = #{deal_price}
                    </if>
                                <if test="synch_state != null">
                        AND id = #{synch_state}
                    </if>
                                <if test="creater != null">
                        AND id = #{creater}
                    </if>
                                <if test="created != null">
                        AND id = #{created}
                    </if>
                                <if test="modified != null">
                        AND id = #{modified}
                    </if>
                                <if test="modifier != null">
                        AND id = #{modifier}
                    </if>
                                <if test="state != null">
                        AND id = #{state}
                    </if>
                                <if test="app_id != null">
                        AND id = #{app_id}
                    </if>
                                <if test="pc_id != null">
                        AND id = #{pc_id}
                    </if>
                                <if test="discount != null">
                        AND id = #{discount}
                    </if>
                                <if test="error_msg != null">
                        AND id = #{error_msg}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select id,channel_id,cms_bt_jm_promotion_id,cms_bt_jm_product_id,product_code,jm_hash_id,limit,market_price,deal_price,synch_state,creater,created,modified,modifier,state,app_id,pc_id,discount,error_msg from cms_bt_jm_promotion_product 
        <where>
                
                    <if test="id != null">
                        AND id = #{id}
                    </if> 
                
                    <if test="channel_id != null">
                        AND id = #{channel_id}
                    </if> 
                
                    <if test="cms_bt_jm_promotion_id != null">
                        AND id = #{cms_bt_jm_promotion_id}
                    </if> 
                
                    <if test="cms_bt_jm_product_id != null">
                        AND id = #{cms_bt_jm_product_id}
                    </if> 
                
                    <if test="product_code != null">
                        AND id = #{product_code}
                    </if> 
                
                    <if test="jm_hash_id != null">
                        AND id = #{jm_hash_id}
                    </if> 
                
                    <if test="limit != null">
                        AND id = #{limit}
                    </if> 
                
                    <if test="market_price != null">
                        AND id = #{market_price}
                    </if> 
                
                    <if test="deal_price != null">
                        AND id = #{deal_price}
                    </if> 
                
                    <if test="synch_state != null">
                        AND id = #{synch_state}
                    </if> 
                
                    <if test="creater != null">
                        AND id = #{creater}
                    </if> 
                
                    <if test="created != null">
                        AND id = #{created}
                    </if> 
                
                    <if test="modified != null">
                        AND id = #{modified}
                    </if> 
                
                    <if test="modifier != null">
                        AND id = #{modifier}
                    </if> 
                
                    <if test="state != null">
                        AND id = #{state}
                    </if> 
                
                    <if test="app_id != null">
                        AND id = #{app_id}
                    </if> 
                
                    <if test="pc_id != null">
                        AND id = #{pc_id}
                    </if> 
                
                    <if test="discount != null">
                        AND id = #{discount}
                    </if> 
                
                    <if test="error_msg != null">
                        AND id = #{error_msg}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select id,channel_id,cms_bt_jm_promotion_id,cms_bt_jm_product_id,product_code,jm_hash_id,limit,market_price,deal_price,synch_state,creater,created,modified,modifier,state,app_id,pc_id,discount,error_msg from cms_bt_jm_promotion_product where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_promotion_product
        set

            `channel_id`=#{channelId}
            ,`cms_bt_jm_promotion_id`=#{cmsBtJmPromotionId}
            ,`cms_bt_jm_product_id`=#{cmsBtJmProductId}
            ,`product_code`=#{productCode}
            ,`jm_hash_id`=#{jmHashId}
            ,`limit`=#{limit}
            ,`market_price`=#{marketPrice}
            ,`deal_price`=#{dealPrice}
            ,`synch_state`=#{synchState}
            ,`modified`=#{modified}
            ,`modifier`=#{modifier}
            ,`state`=#{state}
            ,`app_id`=#{appId}
            ,`pc_id`=#{pcId}
            ,`discount`=#{discount}
            ,`error_msg`=#{errorMsg}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_promotion_product
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_promotion_product
        (
              `channel_id`,
              `cms_bt_jm_promotion_id`,
              `cms_bt_jm_product_id`,
              `product_code`,
              `jm_hash_id`,
              `limit`,
              `market_price`,
              `deal_price`,
              `synch_state`,
              `creater`,
              `created`,
              `modified`,
              `modifier`,
              `state`,
              `app_id`,
              `pc_id`,
              `discount`,
              `error_msg`
        )
        VALUES(
              #{channelId},
              #{cmsBtJmPromotionId},
              #{cmsBtJmProductId},
              #{productCode},
              #{jmHashId},
              #{limit},
              #{marketPrice},
              #{dealPrice},
              #{synchState},
              #{creater},
              #{created},
              #{modified},
              #{modifier},
              #{state},
              #{appId},
              #{pcId},
              #{discount},
              #{errorMsg}
        )
    </insert>
</mapper>