<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmPromotionExportTaskDao">
    <resultMap id="ResultMap" type="CmsBtJmPromotionExportTaskModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,cms_bt_jm_promotion_id,file_name,file_path,error_msg,success_rows,failures_rows,failures_path,error_code,begin_time,end_time,creater,created        from cms_bt_jm_promotion_export_task
        <where>
                <if test="id != null">
                        AND id = #{id}
                    </if>
                                <if test="cms_bt_jm_promotion_id != null">
                        AND id = #{cms_bt_jm_promotion_id}
                    </if>
                                <if test="file_name != null">
                        AND id = #{file_name}
                    </if>
                                <if test="file_path != null">
                        AND id = #{file_path}
                    </if>
                                <if test="error_msg != null">
                        AND id = #{error_msg}
                    </if>
                                <if test="success_rows != null">
                        AND id = #{success_rows}
                    </if>
                                <if test="failures_rows != null">
                        AND id = #{failures_rows}
                    </if>
                                <if test="failures_path != null">
                        AND id = #{failures_path}
                    </if>
                                <if test="error_code != null">
                        AND id = #{error_code}
                    </if>
                                <if test="begin_time != null">
                        AND id = #{begin_time}
                    </if>
                                <if test="end_time != null">
                        AND id = #{end_time}
                    </if>
                                <if test="creater != null">
                        AND id = #{creater}
                    </if>
                                <if test="created != null">
                        AND id = #{created}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select id,cms_bt_jm_promotion_id,file_name,file_path,error_msg,success_rows,failures_rows,failures_path,error_code,begin_time,end_time,creater,created from cms_bt_jm_promotion_export_task 
        <where>
                
                    <if test="id != null">
                        AND id = #{id}
                    </if> 
                
                    <if test="cms_bt_jm_promotion_id != null">
                        AND id = #{cms_bt_jm_promotion_id}
                    </if> 
                
                    <if test="file_name != null">
                        AND id = #{file_name}
                    </if> 
                
                    <if test="file_path != null">
                        AND id = #{file_path}
                    </if> 
                
                    <if test="error_msg != null">
                        AND id = #{error_msg}
                    </if> 
                
                    <if test="success_rows != null">
                        AND id = #{success_rows}
                    </if> 
                
                    <if test="failures_rows != null">
                        AND id = #{failures_rows}
                    </if> 
                
                    <if test="failures_path != null">
                        AND id = #{failures_path}
                    </if> 
                
                    <if test="error_code != null">
                        AND id = #{error_code}
                    </if> 
                
                    <if test="begin_time != null">
                        AND id = #{begin_time}
                    </if> 
                
                    <if test="end_time != null">
                        AND id = #{end_time}
                    </if> 
                
                    <if test="creater != null">
                        AND id = #{creater}
                    </if> 
                
                    <if test="created != null">
                        AND id = #{created}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select id,cms_bt_jm_promotion_id,file_name,file_path,error_msg,success_rows,failures_rows,failures_path,error_code,begin_time,end_time,creater,created from cms_bt_jm_promotion_export_task where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_promotion_export_task
        set

            `cms_bt_jm_promotion_id`=#{cmsBtJmPromotionId}
            ,`file_name`=#{fileName}
            ,`file_path`=#{filePath}
            ,`error_msg`=#{errorMsg}
            ,`success_rows`=#{successRows}
            ,`failures_rows`=#{failuresRows}
            ,`failures_path`=#{failuresPath}
            ,`error_code`=#{errorCode}
            ,`begin_time`=#{beginTime}
            ,`end_time`=#{endTime}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_promotion_export_task
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_promotion_export_task
        (
              `cms_bt_jm_promotion_id`,
              `file_name`,
              `file_path`,
              `error_msg`,
              `success_rows`,
              `failures_rows`,
              `failures_path`,
              `error_code`,
              `begin_time`,
              `end_time`,
              `creater`,
              `created`
        )
        VALUES(
              #{cmsBtJmPromotionId},
              #{fileName},
              #{filePath},
              #{errorMsg},
              #{successRows},
              #{failuresRows},
              #{failuresPath},
              #{errorCode},
              #{beginTime},
              #{endTime},
              #{creater},
              #{created}
        )
    </insert>
</mapper>