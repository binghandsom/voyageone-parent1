<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.jumei.CmsBtJmPromotionImportTaskDao">
    <resultMap id="ResultMap" type="CmsBtJmPromotionImportTaskModel">
        
    </resultMap>
	
    <select id="selectList" resultMap="ResultMap">

        select id,cms_bt_jm_promotion_id,is_import,file_name,error_msg,success_rows,failures_rows,failures_file_Name,error_code,begin_time,end_time,creater,created        from cms_bt_jm_promotion_import_task
        <where>
                <if test="id != null">
                        AND id = #{id}
                    </if>
                                <if test="cmsBtJmPromotionId != null">
                        AND cms_bt_jm_promotion_id = #{cmsBtJmPromotionId}
                    </if>
                                <if test="isImport != null">
                        AND is_import = #{isImport}
                    </if>
                                <if test="fileName != null">
                        AND file_name = #{fileName}
                    </if>
                                <if test="errorMsg != null">
                        AND error_msg = #{errorMsg}
                    </if>
                                <if test="successRows != null">
                        AND success_rows = #{successRows}
                    </if>
                                <if test="failuresRows != null">
                        AND failures_rows = #{failuresRows}
                    </if>
                                <if test="failuresFileName != null">
                        AND failures_file_Name = #{failuresFileName}
                    </if>
                                <if test="errorCode != null">
                        AND error_code = #{errorCode}
                    </if>
                                <if test="beginTime != null">
                        AND begin_time = #{beginTime}
                    </if>
                                <if test="endTime != null">
                        AND end_time = #{endTime}
                    </if>
                                <if test="creater != null">
                        AND creater = #{creater}
                    </if>
                                <if test="created != null">
                        AND created = #{created}
                    </if>
                        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select id,cms_bt_jm_promotion_id,is_import,file_name,error_msg,success_rows,failures_rows,failures_file_Name,error_code,begin_time,end_time,creater,created from cms_bt_jm_promotion_import_task 
        <where>
                
                    <if test="id != null">
                        AND id = #{id}
                    </if> 
                
                    <if test="cmsBtJmPromotionId != null">
                        AND cms_bt_jm_promotion_id = #{cmsBtJmPromotionId}
                    </if> 
                
                    <if test="isImport != null">
                        AND is_import = #{isImport}
                    </if> 
                
                    <if test="fileName != null">
                        AND file_name = #{fileName}
                    </if> 
                
                    <if test="errorMsg != null">
                        AND error_msg = #{errorMsg}
                    </if> 
                
                    <if test="successRows != null">
                        AND success_rows = #{successRows}
                    </if> 
                
                    <if test="failuresRows != null">
                        AND failures_rows = #{failuresRows}
                    </if> 
                
                    <if test="failuresFileName != null">
                        AND failures_file_Name = #{failuresFileName}
                    </if> 
                
                    <if test="errorCode != null">
                        AND error_code = #{errorCode}
                    </if> 
                
                    <if test="beginTime != null">
                        AND begin_time = #{beginTime}
                    </if> 
                
                    <if test="endTime != null">
                        AND end_time = #{endTime}
                    </if> 
                
                    <if test="creater != null">
                        AND creater = #{creater}
                    </if> 
                
                    <if test="created != null">
                        AND created = #{created}
                    </if> 
        </where> limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select id,cms_bt_jm_promotion_id,is_import,file_name,error_msg,success_rows,failures_rows,failures_file_Name,error_code,begin_time,end_time,creater,created from cms_bt_jm_promotion_import_task where id=#{id}
    </select>

     <update id="update">
        update cms_bt_jm_promotion_import_task
        set

            `cms_bt_jm_promotion_id`=#{cmsBtJmPromotionId}
            ,`is_import`=#{isImport}
            ,`file_name`=#{fileName}
            ,`error_msg`=#{errorMsg}
            ,`success_rows`=#{successRows}
            ,`failures_rows`=#{failuresRows}
            ,`failures_file_Name`=#{failuresFileName}
            ,`error_code`=#{errorCode}
            ,`begin_time`=#{beginTime}
            ,`end_time`=#{endTime}
         where id=#{id}   
   </update>

    <delete id="delete">
        delete from  cms_bt_jm_promotion_import_task
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO cms_bt_jm_promotion_import_task
        (
              `cms_bt_jm_promotion_id`,
              `is_import`,
              `file_name`,
              `error_msg`,
              `success_rows`,
              `failures_rows`,
              `failures_file_Name`,
              `error_code`,
              `begin_time`,
              `end_time`,
              `creater`,
              `created`
        )
        VALUES(
              #{cmsBtJmPromotionId},
              #{isImport},
              #{fileName},
              #{errorMsg},
              #{successRows},
              #{failuresRows},
              #{failuresFileName},
              #{errorCode},
              #{beginTime},
              #{endTime},
              #{creater},
              #{created}
        )
    </insert>
</mapper>