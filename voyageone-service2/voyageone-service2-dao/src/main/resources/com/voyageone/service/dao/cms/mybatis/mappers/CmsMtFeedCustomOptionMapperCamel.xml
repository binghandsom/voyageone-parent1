<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.cms.CmsMtFeedCustomOptionDao">
    <resultMap id="ResultMap" type="com.voyageone.service.model.cms.CmsMtFeedCustomOptionModel">

    </resultMap>

    <select id="selectList" resultMap="ResultMap">

        select
        `id`,`channel_id`,`prop_id`,`feed_value_original`,`feed_value_translation`,`created`,`creater`,`modified`,`modifier`
        from cms_mt_feed_custom_option
        <where>
            <if test="id != null">
                AND `id` = #{id}
            </if>
            <if test="channelId != null">
                AND `channel_id` = #{channelId}
            </if>
            <if test="propId != null">
                AND `prop_id` = #{propId}
            </if>
            <if test="feedValueOriginal != null">
                AND `feed_value_original` = #{feedValueOriginal}
            </if>
            <if test="feedValueTranslation != null">
                AND `feed_value_translation` = #{feedValueTranslation}
            </if>
            <if test="created != null">
                AND `created` = #{created}
            </if>
            <if test="creater != null">
                AND `creater` = #{creater}
            </if>
            <if test="modified != null">
                AND `modified` = #{modified}
            </if>
            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select
        `id`,`channel_id`,`prop_id`,`feed_value_original`,`feed_value_translation`,`created`,`creater`,`modified`,`modifier`
        from cms_mt_feed_custom_option
        <where>

            <if test="id != null">
                AND `id` = #{id}
            </if>

            <if test="channelId != null">
                AND `channel_id` = #{channelId}
            </if>

            <if test="propId != null">
                AND `prop_id` = #{propId}
            </if>

            <if test="feedValueOriginal != null">
                AND `feed_value_original` = #{feedValueOriginal}
            </if>

            <if test="feedValueTranslation != null">
                AND `feed_value_translation` = #{feedValueTranslation}
            </if>

            <if test="created != null">
                AND `created` = #{created}
            </if>

            <if test="creater != null">
                AND `creater` = #{creater}
            </if>

            <if test="modified != null">
                AND `modified` = #{modified}
            </if>

            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
        limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`channel_id`,`prop_id`,`feed_value_original`,`feed_value_translation`,`created`,`creater`,`modified`,`modifier` from cms_mt_feed_custom_option where id=#{id}
    </select>

    <update id="update">
        update cms_mt_feed_custom_option
        set

            `channel_id`=#{channelId}
            ,`prop_id`=#{propId}
            ,`feed_value_original`=#{feedValueOriginal}
            ,`feed_value_translation`=#{feedValueTranslation}
            ,`modifier`=#{modifier}
         where id=#{id}
   </update>

    <delete id="delete">
        delete from  cms_mt_feed_custom_option
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_mt_feed_custom_option
        (
              `channel_id`,
              `prop_id`,
              `feed_value_original`,
              `feed_value_translation`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{channelId},
              #{propId},
              #{feedValueOriginal},
              #{feedValueTranslation},
              #{created},
              #{creater},
              #{modified},
              #{modifier}
        )
    </insert>
</mapper>
