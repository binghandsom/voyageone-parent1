<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voyageone.service.dao.cms.CmsMtPlatformDictDao">
    <!--<resultMap id="ResultMap" type="CmsMtDictModel">-->
    <resultMap id="ResultMap" type="com.voyageone.service.model.cms.CmsMtPlatFormDictModel">

    </resultMap>

    <select id="selectList" resultMap="ResultMap">

        select `id`,`order_channel_id`,`cart_id`,`name`,`value`,`comment`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_platform_dict
        <where>
            <if test="id != null">
                AND `id` = #{id}
            </if>
            <if test="orderChannelId != null">
                AND `order_channel_id` = #{orderChannelId}
            </if>
            <if test="cartId != null">
                AND `cart_id` = #{cartId}
            </if>
            <if test="name != null">
                AND `name` = #{name}
            </if>
            <if test="value != null">
                AND `value` = #{value}
            </if>
            <if test="comment != null">
                AND `comment` = #{comment}
            </if>
            <if test="created != null">
                AND `created` = #{created}
            </if>
            <if test="creater != null">
                AND `creater` = #{creater}
            </if>
            <if test="modified != null">
                AND `modified` = #{modified}
            </if>
            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
    </select>
    <select id="selectOne" resultMap="ResultMap">
        select `id`,`order_channel_id`,`cart_id`,`name`,`value`,`comment`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_platform_dict
        <where>

            <if test="id != null">
                AND `id` = #{id}
            </if>

            <if test="orderChannelId != null">
                AND `order_channel_id` = #{orderChannelId}
            </if>

            <if test="cartId != null">
                AND `cart_id` = #{cartId}
            </if>

            <if test="name != null">
                AND `name` = #{name}
            </if>

            <if test="value != null">
                AND `value` = #{value}
            </if>

            <if test="comment != null">
                AND `comment` = #{comment}
            </if>

            <if test="created != null">
                AND `created` = #{created}
            </if>

            <if test="creater != null">
                AND `creater` = #{creater}
            </if>

            <if test="modified != null">
                AND `modified` = #{modified}
            </if>

            <if test="modifier != null">
                AND `modifier` = #{modifier}
            </if>
        </where>
        limit 1
    </select>
    <select id="select" resultMap="ResultMap">
        select `id`,`order_channel_id`,`cart_id`,`name`,`value`,`comment`,`created`,`creater`,`modified`,`modifier` from
        cms_mt_platform_dict where id=#{id}
    </select>

    <update id="update">
        update cms_mt_platform_dict
        set

            `order_channel_id`=#{orderChannelId}
            ,`cart_id`=#{cartId}
            ,`name`=#{name}
            ,`value`=#{value}
            ,`comment`=#{comment}
            ,`modifier`=#{modifier}
         where id=#{id}
   </update>

    <delete id="delete">
        delete from cms_mt_platform_dict
        where id=#{id}

    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_mt_platform_dict
        (
              `order_channel_id`,
              `cart_id`,
              `name`,
              `value`,
              `comment`,
              `created`,
              `creater`,
              `modified`,
              `modifier`
        )
        VALUES(
              #{orderChannelId},
              #{cartId},
              #{name},
              #{value},
              #{comment},
        now(),
              #{creater},
        now(),
        #{creater}
        )
    </insert>
</mapper>
